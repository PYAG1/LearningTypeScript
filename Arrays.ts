const superft:string[]= []

//the superft is a type of string array 

type USer = {
    name:string;
    isActive:boolean
}

const allUsers: USer[] =[]
//this means the allUsers is of type User so that should be the info added tp the array 

const Mlmodels: number[][]= [
    
]
 
