//to restrict a choice in the case of a shooping app
const asile=0;
const windo=1;

enum Seatchoice {
    aisle,
    middle,
    windo
}
//we declare enums by using enum
//the first value start woth 0
// it works with increments of one
//you can use it with string s